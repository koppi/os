/* <io.h> is a Windows header; m_misc.c includes it only under _WIN32. This
 * stub exists so the include resolves if that guard is ever relaxed. */
#ifndef DOOM_SHIM_IO_H
#define DOOM_SHIM_IO_H
#endif
