/* <strings.h> for the koppi-os Doom port: doomtype.h includes it for the
 * case-insensitive comparisons, which live in string.c. */
#ifndef DOOM_SHIM_STRINGS_H
#define DOOM_SHIM_STRINGS_H
#include <string.h>
#endif
